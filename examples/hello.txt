fn main(argc: i32) -> i32 {
    let x: i32 = max(3, 4);
    return add(x, square(2));
}

fn square(x: i32) -> i32 {
    return x * x;
}

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn max(a: i32, b: i32) -> i32 {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}