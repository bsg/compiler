fn main(argc: i32) -> i32 {
    let x: i32 = 10 * 2;
    return add(x, square(2));
}

fn square(x: i32) -> i32 {
    return x * x;
}

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn foo(a: bool) -> bool {
    return a;
}